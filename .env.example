DASHBOARD_ENV=dev
DASHBOARD_TIMEZONE=Europe/Berlin
DASHBOARD_CONFIG_PATH=config/dashboard.yaml
DASHBOARD_DB_PATH=data/dashboard.db
# Optional: TheSportsDB API key (default test key shown)
SPORTS_API_KEY=3

# Optional: keep private URLs in a local-only config file
# DASHBOARD_CONFIG_PATH=config/dashboard.local.yaml
