ui:
  title: "My Dashboard"
  layout:
    left_column_width: "30%"
    right_rotation_seconds: 60
  photo_rotation_seconds: 120
refresh:
  interval_minutes: 10
  jitter_seconds: 15
location:
  mode: "auto"
  fallback_city: "Stuttgart, DE"
weather:
  provider: "open_meteo"
  units: "metric"
  show_daily_days: 5
